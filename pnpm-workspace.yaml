packages:
  - "apps/*"
  - "packages/*"

# Dependencias nativas con scripts de build aprobados explícitamente (política supply-chain de pnpm).
# sharp: análisis/optimización de imágenes (usado por Next.js y por el motor de verificación, Épica 6).
allowBuilds:
  sharp: true
