# E01-T15 · README raíz del monorepo

## Goal
README con comandos del monorepo, mapa de apps/packages y cómo correr los checks.

## Acceptance Criteria
- [x] `README.md` en raíz documenta setup (`pnpm install`, `.env`), comandos raíz (build/typecheck/lint/test/dev/format) y cómo filtrar por proyecto.
- [x] Incluye el mapa de `apps/*` y `packages/*` y las convenciones clave (centavos, UTC/Bogotá, ESM/D8).
- [x] Enlaza al PRD, decisiones y CI.

## Notes
- Implementado en `README.md`.
